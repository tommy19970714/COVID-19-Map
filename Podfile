# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CoronaMap' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CoronaMap
  pod 'Alamofire'
  pod 'FloatingPanel'
  pod 'SDWebImage'
  target 'CoronaMapTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
